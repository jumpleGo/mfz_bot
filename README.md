# 🤖 MoneyFlowZen Telegram Bot

Telegram бот для управления подписками с интеграцией Firebase Realtime Database и криптовалютными платежами.

## 📋 Функционал

- ✅ Отображение активных тарифов из Firebase
- 💳 Выбор метода оплаты (Bybit UID, USDT)
- 📸 Загрузка чека об оплате
- 👨‍💼 Админская панель для подтверждения платежей
- 🔗 Автоматическая генерация одноразовых ссылок на канал
- 📋 Просмотр текущей подписки с полной информацией
- ⏰ Отображение времени до истечения подписки
- ⚠️ Уведомления о скором истечении подписки
- 📨 Система очереди сообщений для рассылок из админки
- 🎯 Отправка сообщений конкретному пользователю или группе
- 📊 Запись всех платежей и пользователей в Firebase

## 🚀 Установка

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте Firebase

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект `moneyflowzen`
3. Перейдите в **Project Settings** → **Service Accounts**
4. Нажмите **Generate New Private Key**
5. Скачайте JSON файл и сохраните его как `firebase-service-account.json` в корне проекта

### 3. Создайте Telegram бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Скопируйте токен

### 4. Получите ID администратора

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш ID

### 5. Получите ID канала

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в ваш канал
2. Перешлите любое сообщение из канала боту
3. Скопируйте ID канала (должен начинаться с `-100`)
4. **ВАЖНО:** Добавьте вашего бота в канал как администратора с правом "Invite users via link"

### 6. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные в `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_TELEGRAM_ID=your_admin_id
FIREBASE_DATABASE_URL=https://moneyflowzen-default-rtdb.firebaseio.com/
CHANNEL_ID=your_channel_id
```

## 🏃 Запуск

```bash
npm start
```

Или в режиме разработки:

```bash
npm run dev
```

## 📁 Структура проекта

```
mfz_bot/
├── bot.js                          # Основной файл бота
├── config/
│   └── firebase.js                 # Конфигурация Firebase
├── services/
│   ├── tariffService.js            # Работа с тарифами
│   └── paymentService.js           # Работа с платежами
├── utils/
│   └── keyboards.js                # Клавиатуры для бота
├── .env                            # Переменные окружения (не в git)
├── .env.example                    # Пример переменных
├── firebase-service-account.json   # Firebase credentials (не в git)
├── package.json
└── README.md
```

## 🗄️ Структура Firebase

### `tariffsForPayment`
```json
{
  "tariffId": {
    "name": "Название тарифа",
    "price": 1000,
    "currencyCode": "₽",
    "active": true,
    "description": "Подробное описание тарифа, которое будет показано пользователю перед оплатой (опционально)"
  }
}
```

### `paymentData`
```json
{
  "bybit_uid": {
    "address": "12345678"
  },
  "USDT": {
    "address": "TXyz123..."
  }
}
```

### `bot_users`
```json
{
  "123456789": {
    "userId": 123456789,
    "username": "username",
    "firstName": "Имя",
    "lastName": "Фамилия",
    "languageCode": "ru",
    "isBot": false,
    "createdAt": "2025-11-23T00:00:00.000Z",
    "lastInteraction": "2025-11-23T01:00:00.000Z",
    "updatedAt": "2025-11-23T01:00:00.000Z"
  }
}
```

### `payments`
```json
{
  "paymentKey": {
    "id": "52U-dYAr",
    "key": "-OeWzHBHvW6B52U-dYAr",
    "createdAt": "2025-11-20T15:58:32.705Z",
    "updatedAt": "2025-11-20T16:00:52.785Z",
    "expiresAt": "2025-11-20T16:28:32.705Z",
    "crypto": "bybit_uid",
    "price": 15,
    "currencyCode": "₽",
    "status": "payed",
    "tariffId": "altsWatcher",
    "tariffName": "Зритель",
    "userTelegram": "username",
    "userId": 123456789,
    "receiptPhotoId": "AgACAgIAAxkBAAI...",
    "inviteLink": "https://t.me/+xyz123...",
    "inviteLinkCreatedAt": "2025-11-20T16:00:52.785Z"
  }
}
```

## 🔄 Процесс работы

### Оформление подписки:
1. **Пользователь** нажимает `/start` (автоматически сохраняется в `bot_users`)
2. Выбирает **"Выбрать подписку"**
3. Бот загружает активные тарифы из Firebase (`tariffsForPayment`)
4. **Пользователь** выбирает тариф
5. Бот показывает название, цену, описание тарифа и доступные методы оплаты из Firebase (`paymentData`)
6. **Пользователь** выбирает метод оплаты (Bybit UID или USDT)
7. Бот показывает реквизиты и создает запись в `payments`
8. **Пользователь** производит оплату и загружает чек
9. **Админу** приходит уведомление с чеком
10. **Админ** подтверждает или отклоняет платеж
11. При подтверждении **пользователю** отправляется одноразовая ссылка на канал
12. **Пользователь** присоединяется и использует команду `/joined` для отзыва ссылки

### Просмотр подписки:
- Нажать **"Моя подписка"** в главном меню
- Бот покажет:
  - Название тарифа и варианта
  - Стоимость и способ оплаты
  - Дату оформления и окончания
  - Количество дней до истечения
  - Предупреждение, если подписка скоро истечет (≤3 дней)

## 🔗 Механизм одноразовой ссылки

Для обеспечения безопасности и предотвращения повторного использования ссылок реализован **комбинированный механизм**:

### 🎯 Три способа отзыва ссылки:

1. **Автоматический отзыв по таймеру (основной):**
   - Ссылка **автоматически отзывается через 30 минут** после создания
   - Этого времени достаточно для присоединения пользователя
   - Предотвращает передачу ссылки другим людям
   - Работает надежно для всех типов чатов (каналы и группы)

2. **Ручной отзыв командой `/joined`:**
   - После присоединения к каналу пользователь использует команду `/joined` в боте
   - Бот проверяет членство в канале через API
   - Если проверка успешна - ссылка отзывается сразу
   - Пользователь получает подтверждение

3. **Автоматическое истечение:**
   - Дополнительная защита: `expire_date` установлен на 24 часа
   - Даже если отзыв не сработал, ссылка станет недействительной

### 📋 Процесс работы:

1. **Пользователь получает ссылку** с инструкцией
2. **Переходит по ссылке** и присоединяется к каналу
3. **Выполняет команду `/joined`** в боте
4. **Ссылка отзывается** - никто другой не сможет использовать её

**⚠️ Важно:** Если пользователь не выполнит `/joined`, ссылка всё равно отзовётся автоматически через 30 минут.

### 💡 Почему не используется `chat_member`:

Telegram API **не отправляет** обновления `chat_member` для присоединения подписчиков к каналам (только для групп). Поэтому реализован более надежный комбинированный подход с таймером и ручным подтверждением.

## ⚙️ Статусы платежей

- `pending` - ожидает проверки
- `payed` - подтвержден
- `rejected` - отклонен

## 🔒 Безопасность

- ✅ Firebase credentials в `.gitignore`
- ✅ Переменные окружения в `.env`
- ✅ Трехуровневая защита пригласительных ссылок:
  - Автоматический отзыв через 30 минут
  - Ручной отзыв командой `/joined` с проверкой членства
  - Автоматическое истечение через 24 часа
- ✅ Подтверждение платежей админом
- ✅ Сохранение всех операций в Firebase для аудита

## 📨 Система очереди сообщений

Бот поддерживает отправку сообщений через Firebase Realtime Database.
Подробная документация: [docs/MESSAGE_QUEUE.md](./docs/MESSAGE_QUEUE.md)

### Возможности админки

Из веб-интерфейса админки можно:
- 📋 Просматривать всех пользователей бота из таблицы `bot_users`
- 💬 Отправлять сообщения конкретному пользователю
- 📢 Отправлять массовую рассылку выбранным пользователям
- 🎯 Фильтровать получателей (все/с подпиской/без подписки)
- 🎨 Использовать HTML/Markdown форматирование
- 📊 Просматривать историю отправленных сообщений и статистику

### Быстрый старт

1. Создайте запись в `message_queue`:
```javascript
// Отправка одному пользователю
{
  "type": "single",
  "target": { "userId": "123456789" },
  "message": "Привет!",
  "parseMode": null,
  "createdAt": "2025-11-28T...",
  "createdBy": "admin@email.com",
  "status": "pending"
}

// Массовая рассылка
{
  "type": "broadcast",
  "target": { "filter": "all" },
  "message": "Всем привет!",
  "parseMode": "HTML",
  "createdAt": "2025-11-28T...",
  "createdBy": "admin@email.com",
  "status": "pending"
}
```

2. Бот автоматически обработает сообщение
3. Статус изменится на `completed` с полной статистикой

## 🐛 Troubleshooting

### Бот не запускается
- Проверьте наличие файла `firebase-service-account.json`
- Проверьте правильность токена бота в `.env`
- Убедитесь, что все зависимости установлены: `npm install`

### Не генерируются ссылки на канал
- Убедитесь, что бот добавлен в канал как администратор
- Проверьте, что у бота есть право "Invite users via link"
- Проверьте правильность `CHANNEL_ID` в `.env`

### Сообщения из очереди не отправляются
- Проверьте логи бота - должно быть `👂 Слушатель очереди сообщений запущен...`
- Убедитесь, что статус сообщения `pending`
- Проверьте структуру записи в Firebase

### Не приходят уведомления админу
- Проверьте правильность `ADMIN_TELEGRAM_ID` в `.env`
- Убедитесь, что вы написали боту хотя бы один раз

### Ссылки не отзываются

**Механизм отзыва:**
- Ссылки отзываются автоматически через **30 минут** после создания
- Пользователь может отозвать ссылку сразу командой `/joined` после присоединения
- Это нормальное поведение и не требует дополнительной настройки

**Если нужно изменить время автоотзыва:**
1. Откройте `bot.js`
2. Найдите строку: `}, 30 * 60 * 1000); // 30 минут`
3. Измените `30` на нужное количество минут

**Проверка:**
- В логах должно появляться: `📝 Создана invite ссылка для пользователя...`
- Через 30 минут: `⏰ Таймер истек для пользователя...`
- При команде `/joined`: `✅ Ссылка вручную отозвана пользователем...`

## 📝 Лицензия

MIT
